<?xml version="1.0" encoding="utf-8"?>
<!-- One station summarised on the page 3 charging card: name, then operator, distance and
     power on a second line. Included three times, so the ids live on the <include> tag. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/summary_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/secondary_text_size"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/summary_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/text_secondary"
        android:textSize="@dimen/secondary_text_size" />
</LinearLayout>
