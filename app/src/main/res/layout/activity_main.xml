<?xml version="1.0" encoding="utf-8"?>
<!-- Swipeable two-page home carousel (page 1: launcher home, page 2: info cards) with a
     SAIC-style bar pagination indicator pinned to the bottom centre. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/home_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="@dimen/page_indicator_margin_bottom"
        android:orientation="horizontal">

        <View
            android:id="@+id/page_bar_0"
            android:layout_width="@dimen/page_indicator_active_width"
            android:layout_height="@dimen/page_indicator_height"
            android:layout_marginEnd="10dp"
            android:background="@drawable/page_bar_active" />

        <View
            android:id="@+id/page_bar_1"
            android:layout_width="@dimen/page_indicator_inactive_width"
            android:layout_height="@dimen/page_indicator_height"
            android:background="@drawable/page_bar_inactive" />
    </LinearLayout>
</FrameLayout>
